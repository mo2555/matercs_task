enum RequestStatus {
  initial,
  loading,
  loaded,
  error,
}

enum LoginMethod {
  id,
  phone,
  google,
  facebook,
}


enum AnimationType {
  svga,
  mp4,
  vap,
  image,
  none,
}

enum ImageType {
  url,
  asset,
  text,
}

enum ApiCallType {
  get,
  post,
  postFile,
  put,
  patch,
  delete,
}